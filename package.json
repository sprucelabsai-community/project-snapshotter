{
  "name": "regressionproof",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build.dev --stream",
    "build.ci": "lerna run build.ci --stream",
    "build.dev": "lerna run build.dev --stream",
    "build.dist": "lerna run build.dist --stream",
    "clean": "lerna run clean",
    "publish": "lerna publish",
    "clean.all": "yarn clean.dependencies && yarn clean.build",
    "clean.build": "for d in packages/*; do echo $d; cd $d; yarn clean.build; cd ../..; done",
    "clean.dependencies": "rm -rf node_modules/ package-lock.json yarn.lock && for d in packages/*; do echo $d; cd $d; yarn clean.dependencies; cd ../..; done",
    "rebuild": "yarn clean.all && yarn && yarn build.dev",
    "watch": "concurrently -n snap,api,client,cli,jest -c blue,green,yellow,magenta,cyan \"yarn workspace @regressionproof/snapshotter watch.build.dev\" \"yarn workspace @regressionproof/api watch.build.dev\" \"yarn workspace @regressionproof/client watch.build.dev\" \"yarn workspace @regressionproof/cli watch.build.dev\" \"yarn workspace @regressionproof/jest-reporter watch.build.dev\"",
    "watch.build.dev": "lerna run watch.build.dev --stream --parallel",
    "test": "lerna run test --stream --concurrency=1",
    "lint": "lerna run lint",
    "fix.lint": "lerna run fix.lint",
    "build.resolve-paths": "lerna run build.resolve-paths",
    "upgrade.packages": "yarn add -D eslint lerna @sprucelabs/resolve-path-aliases @sprucelabs/semantic-release -W ; for d in packages/*; do echo $d; cd $d; spruce upgrade || true; cd ../..; done",
    "upgrade.packages.all": "yarn install && yarn upgrade.packages",
    "update.dependencies": "yarn clean.dependencies && yarn; yarn add -D lerna -W ; for d in packages/*; do echo $d; cd $d; spruce update.dependencies; cd ../..; done",
    "cli": "node packages/cli/build/cli.js",
    "dev": "./scripts/boot-api.sh",
    "setup.e2e": "./scripts/e2e-setup.sh",
    "build.copy-files": "true",
    "build.tsc": "true",
    "lint.tsc": "true",
    "post.watch.build": "true",
    "resolve-paths.lint": "true",
    "watch.tsc": "true",
    "watch.rebuild": "true"
  },
  "devDependencies": {
    "@sprucelabs/resolve-path-aliases": "^4.0.13",
    "@sprucelabs/semantic-release": "^6.0.0",
    "concurrently": "^9.1.2",
    "eslint": "^9.39.2",
    "lerna": "^9.0.3"
  },
  "dependencies": {
    "@sprucelabs/error": "^8.1.0"
  }
}
